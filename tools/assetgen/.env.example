# Copy to tools/assetgen/.env (gitignored) or set in the repo-root .env.
# Vertex AI express-mode API key for Nano Banana (Gemini image) generation.
GOOGLE_CLOUD_API_KEY=
# PixelLab API key (ships + goods icons; used by the PixelLab MCP / REST client).
PIXELLAB_API_KEY=
